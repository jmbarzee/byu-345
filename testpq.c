#include <stdio.h>

/*#include "pq.h"

int main(int argc, char* argv[]);

PQ* rQueue;						// task ready queue

int taskPriority(Tid tid);

int taskPriority(Tid tid) {
	return tid;
}

int main(int argc, char* argv[]) {
	printf("Starting Test Main\n");
	rQueue = newPqueue(5, "ready Queue");
	PQ* pq = newPqueue(5, "testQueue");
	printPqueue(pq);
	put(pq, 0);
	printPqueue(pq);
	put(pq, 1);
	printPqueue(pq);
	put(pq, 2);
	printPqueue(pq);
	put(pq, 3);
	printPqueue(pq);
	put(pq, 4);
	printPqueue(pq);
	put(pq, 5);
	printPqueue(pq);
	put(pq, 6);
	printPqueue(pq);
	ready(pq, rQueue);
	printPqueue(pq);
	printPqueue(rQueue);
	ready(pq, rQueue);
	printPqueue(pq);
	printPqueue(rQueue);
	ready(pq, rQueue);
	printPqueue(pq);
	printPqueue(rQueue);
	ready(pq, rQueue);
	printPqueue(pq);
	printPqueue(rQueue);
	block(rQueue, pq, 3);
	printPqueue(pq);
	printPqueue(rQueue);
	block(rQueue, pq, 5);
	printPqueue(pq);
	printPqueue(rQueue);
	block(rQueue, pq, 4);
	printPqueue(pq);
	printPqueue(rQueue);
}
// */

